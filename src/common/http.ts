// TODO: Add AppError class and asyncHandler wrapper here if you want.
// Example shape:
// export class AppError extends Error { constructor(public statusCode: number, message: string){ super(message) } }
// export const asyncHandler = (fn: any) => (req: any, res: any, next: any) => Promise.resolve(fn(req,res,next)).catch(next);
